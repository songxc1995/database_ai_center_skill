interface:
  display_name: "Database AI Center"
  short_description: "Analyze database alerts and optionally enrich with Zabbix facts"
  default_prompt: "Analyze live Database AI Center alerts and, when host-side evidence is needed, use zabbix-readonly before returning the final structured diagnosis."
